//_DIT is op (Thor)
//DAH_ is a
//DIT_ is b
//op_ is c
//REPLACED THESE TWO #define DIT (
//REPLACED THESE TWO #define DAH )
//REPLACED with ++
//REPLACED with *
//REPLACED with for
//REPLACED with malloc
//REPLACED with gets
//REPLACED with char
char _a[]="ETIANMSURWDKGOHVFaLaPJBXCYZQb54a3d2f16g7c8a90l?e'b.s;i,d:";
main()
{
	char *op, *a, *b, *c, *malloc(), *gets();
	for (op = malloc(81), b = op++; op == gets(op); _op('\n'))
		for (a = op; *a; _op(*c ? ++(*b) : '?'), _op(' '), a++)
			for(*b = 2, c =_a; *c && (*c != (*a >= 'a' ? *a &223 : *a)); (*b)++, c++
				*b += (*c >= 'a' ? * c -'a': 0);
}

_DAH (b)
{ 	_op(b > 3 ? _DAH ( b >> 1 ):'\0');
	return b &1 ? '-':'.';
}
_op(b) 
char b;
{
	(void) write(1, &b, 1);
}
